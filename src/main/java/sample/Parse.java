package sample;

import java.util.HashMap;
import java.util.HashSet;

public class Parse {

    Indexer idxr;

    public Parse() {
        idxr = new Indexer();
    }

    public void ParseDoc(String text, String id) {
        //HashMap contains the terms of the document, and the document's id
        HashMap<Term, String> FileDict = new HashMap<>();

    }
}
